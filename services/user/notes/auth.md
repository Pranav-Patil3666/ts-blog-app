# typescript authentication

- you created controllers first for login , syntax and different things are noted in controller

- to not repeat same things you created try catch handler ,different filer so you directly import and do not need to specify that request is of type Request etc etc.

-now to update profile pic you use cloudinary
    
    -we first set the api key , our cloud name , secret in .env file and a basic setup in server.ts

    -then we created a multer.ts in middleware and a buffergenerator in utils -- you already know why



